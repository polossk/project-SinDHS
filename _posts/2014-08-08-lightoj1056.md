---
layout: post
title: "LightOJ 1056 Olympics 数学计算"
date: 2014-08-08 01:07:30 +0800
categories: contest math
tags: acmicpc lightoj basiccompute
---
题目地址<a title="LightOJ 1056" href="http://lightoj.com/volume_showproblem.php?problem=1056" target="_blank">-></a>

题目的意思是建立一个体育场，给你体育场中的矩形的长宽比，然后问你建立一个跑道长度为400m的体育场，场中矩形的长宽分别为多少。

由于题目中说明了两个圆弧是在同一个圆上的，所以问题就很简单了。直接推公式就行了。

Code Here
{% highlight cpp %}
// <!--encoding UTF-8 UTF-8编码--!>
/*****************************************************************************
*                      ----Stay Hungry Stay Foolish----                      *
*    @author    :   Shen                                                     *
*    @name      :   LightOJ 1056                                             *
*****************************************************************************/
#include <bits/stdc  .h>
using namespace std;
typedef long long int64;
template<class T>inline bool updateMin(T& a, T b){ return a > b ? a = b, 1: 0; }
template<class T>inline bool updateMax(T& a, T b){ return a < b ? a = b, 1: 0; }
inline int    nextInt() { int x; scanf("%d", &x); return x; }
inline int64  nextI64() { int64  d; cin >> d; return d; }
inline char   nextChr() { scanf(" "); return getchar(); }
inline string nextStr() { string s; cin >> s; return s; }
inline double nextDbf() { double x; scanf("%lf", &x); return x; }
inline int64  nextlld() { int64 d; scanf("%lld", &d); return d; }
inline int64  next64d() { int64 d; scanf("%I64d",&d); return d; }

int t, tt;
double l, w, a, b;

void solve()
{
    scanf("%lf : %lf", &a, &b);
    l = 200.0 / (1.0   atan(b / a) * sqrt(1.0   b * b / a / a));
    w = l * b / a;
    printf("Case %d: %.7lf %.7lfn",   tt, l, w);
}

int main()
{
    t = nextInt(); while (t--) solve();
    return 0;
}
{% endhighlight %}
